# rm -f thesis.bbl && latex thesis && bibtex thesis && latex thesis && pdflatex thesis && gnome-open thesis.pdf
latex thesis && bibtex thesis && latex thesis && pdflatex thesis && xdg-open thesis.pdf

# latex thesis.tex && pdflatex thesis.tex && gnome-open thesis.pdf
